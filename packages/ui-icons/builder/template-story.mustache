import * as React from 'react';
import '../assets/css/styles.css';
import * as IconsList from './index';
import { message } from 'antd';
// @ts-ignore https://github.com/nkbt/react-copy-to-clipboard/issues/105
import { CopyToClipboard } from 'react-copy-to-clipboard';

export default {
    title: 'Icons',
};

const copyIcon = (iconName: string) => {
    message.success(`<${iconName}/> copied 🎉`);
};

export const Library = () => (
    <main className="content">
        <h1>Icons</h1>

        <section className="icons-group">
            <h2>16px</h2>
            <div className="icons-group-content">
                {{#icons16}}
                <CopyToClipboard text="<{{iconName}}/>" onCopy={() => copyIcon('{{iconName}}')}>
                    <IconsList.{{iconName}} className="icon"/>
                </CopyToClipboard>
                {{/icons16}}
            </div>
        </section>

        <section className="icons-group">
            <h2>24px</h2>
            <div className="icons-group-content">
                {{#icons24}}
                <CopyToClipboard text="<{{iconName}}/>" onCopy={() => copyIcon('{{iconName}}')}>
                    <IconsList.{{iconName}} className="icon"/>
                </CopyToClipboard>
                {{/icons24}}
            </div>
        </section>
    </main>
);
Library.story = {
    title: 'Library'
};
